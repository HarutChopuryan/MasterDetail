<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MasterDetail.Forms.Pages.PhotoPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Button
            Grid.Row="0"
            Grid.Column="0"
            Clicked="OnTakeButtonClicked"
            Text="Take Photo" />
        <Button
            Grid.Row="0"
            Grid.Column="1"
            Clicked="OnPickButtonClicked"
            Text="Pick Photo" />
        <Image
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Source="{Binding ImageSource, Mode=TwoWay}" />
    </Grid>
    <ContentPage.ToolbarItems>
        <ToolbarItem
            Clicked="OnPhotoDoneClicked"
            Order="Default"
            Text="Done" />
    </ContentPage.ToolbarItems>
</ContentPage>